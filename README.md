PHP development environment with nginx, PHP, MariaDB and phpMyAdmin

It runs executing:
  docker-compose up

It stops executing
  docker-compose down

Always execute these commands from the folder that contains the docker-compose.yml file
